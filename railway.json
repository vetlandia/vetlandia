{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash scripts/backup_before_migrate.sh; alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
